package dev.pabllopf.ecommerceprice;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main entry point for the E-Commerce Price application.
 * This is a Spring Boot application that manages pricing-related operations.
 */
@SpringBootApplication
public class ECommercePriceApplication {

    /**
     * Main method to launch the Spring Boot application.
     *
     * @param args Command-line arguments passed to the application.
     */
    public static void main(String[] args) {
        SpringApplication.run(ECommercePriceApplication.class, args);
    }
}
